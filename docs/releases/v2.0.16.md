# v2.0.16

## Summary

- Release date: 2026-07-13
- Release issue: `manual-safe-release`
- Source PR issue: `n/a`
- Source PR: `n/a`

## Included

- Search API: hardened `search_serp` ad-domain normalization so the server now prefers validated advertiser hosts from structured URLs, keeps `product_gallery` domains unresolved, and rejects confident-wrong dotted brand tokens such as `dr.head`.
- Search API: confirmed on live Yandex Search API control queries that redirect hosts no longer leak into `ads[].domain` and unresolved ads now fail closed instead of becoming false competitors.
- Automation: hardened release follow-up creation so release metadata is carried as structured machine fields and no longer depends on prose parsing plus mtime-based workspace selection.

## Validation

- `.venv/bin/python -m compileall -q src scripts`
- `.venv/bin/python -m pytest -q`
- `.venv/bin/python scripts/agent_lint.py`
- `.venv/bin/python scripts/live_validation.py --suite direct,metrica,wordstat,search`
- `.venv/bin/python scripts/release_guard.py --version 2.0.16 --require-release-notes`
